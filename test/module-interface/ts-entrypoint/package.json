{
  "name": "ts-entrypoint-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npm run prepare-deps && npm run build && node index.js",
    "prepare-deps": "npm i --no-save --no-package-lock",
    "build": "npx tsc"
  },
  "author": "Jan Schwalbe",
  "license": "MIT",
  "dependencies": {
    "@cdxoo/dbscan": "file:../../..",
    "@types/node": "^20.3.1"
  },
  "devDependencies": {
    "typescript": "^5.1.3"
  }
}
